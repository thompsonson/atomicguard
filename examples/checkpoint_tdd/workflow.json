{
  "name": "checkpoint-tdd-demo",
  "description": "TDD workflow demonstrating checkpoint/resume with config files",
  "specification": "Create a Stack class with push, pop, peek, is_empty methods. The Stack should follow LIFO (Last-In-First-Out) semantics.",
  "constraints": "Use Python. Raise IndexError on underflow (pop/peek on empty stack).",
  "rmax": 2,
  "action_pairs": {
    "g_test": {
      "generator": "MockTestGenerator",
      "guard": "syntax"
    },
    "g_impl": {
      "generator": "MockImplGenerator",
      "guard": "dynamic_test",
      "requires": ["g_test"]
    }
  }
}
