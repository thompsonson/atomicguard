{
  "name": "G_plan Validation Benchmark",
  "description": "Plan generation and validation using the G_plan taxonomy. Supports both deterministic (PlanGenerator) and LLM-backed (LLMPlanGenerator) plan generation.",
  "model": "qwen2.5-coder:14b",
  "rmax": 3,
  "action_pairs": {
    "g_plan": {
      "generator": "PlanGenerator",
      "generator_config": {
        "plan_source": "sdlc_v2",
        "defect": null
      },
      "guard": "plan_medium",
      "description": "Load a workflow plan from catalog and validate at configured rigor level"
    },
    "g_plan_llm": {
      "generator": "LLMPlanGenerator",
      "guard": "plan_medium",
      "description": "Generate a workflow plan via LLM and validate at configured rigor level"
    }
  }
}
